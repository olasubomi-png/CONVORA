/**
 * Phase 0: no domain tables.
 *
 * Phase 1 will introduce organization-scoped tables here.
 * Export schema objects from this module so drizzle-kit and the
 * database client share a single source of truth.
 */
export const schema = {};
